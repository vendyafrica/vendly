# nixpacks.toml (place at repository root)
[phases.setup]
nixPkgs = ["nodejs_18", "pnpm"]

[phases.install]
cmds = [
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = [
  "pnpm --filter ./apps/api... run build"
]

[phases.start]
cmds = [
  "pnpm --filter ./apps/api... start"
]
