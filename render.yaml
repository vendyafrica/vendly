services:
  - type: web
    name: vendly-api
    runtime: node
    plan: free 
    region: oregon 

    buildCommand: pnpm install && pnpm build --filter api
    startCommand: node apps/api/dist/server.js

    buildFilter:
      paths:
        - apps/api/**
        - packages/**
        - package.json
        - pnpm-lock.yaml
        - turbo.json
      ignoredPaths:
        - apps/web/**
        - apps/storefront/**
        - apps/admin/**
        - README.md

    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
   